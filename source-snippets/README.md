# Source excerpts

These excerpts show only the measurement mechanisms needed to interpret the evidence. They omit endpoint construction, authentication, account lookup, database credentials, request bodies, response bodies, deployment configuration, and key material.

The excerpts should be compared with the tagged source revisions named in `evidence/evidence-summary.json`. They are explanatory extracts, not a buildable application or backend.

